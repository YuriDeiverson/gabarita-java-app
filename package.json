{
  "name": "gabarita-ai",
  "private": true,
  "version": "1.0.0",
  "description": "Sistema de estudos para concursos com metodologia Pareto 80/20",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend:local\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "npm run dev:backend:local",
    "dev:backend:local": "cd backend && mvn spring-boot:run",
    "dev:backend:docker": "docker compose up api postgres",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "docker compose build api",
    "build": "npm run build:frontend && npm run build:backend",
    "start": "docker compose up -d",
    "stop": "docker compose down",
    "logs:backend": "docker compose logs -f api",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "clean": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}

